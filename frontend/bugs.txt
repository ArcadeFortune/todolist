using autoclicker on the add button destroys the backend
# /available_tasks doesnt truly return the next available task, bc its using the total size of the collection.
clicking the add button doesnt reset the textfield
# /available_tasks can only reach up to ten tasks xd